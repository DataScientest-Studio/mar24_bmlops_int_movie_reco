version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - model_data:/app/models
      - app_data:/app/src/data/data


volumes:
  model_data:
  app_data: